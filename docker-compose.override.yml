version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: make dev
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app
      - /app/node_modules

